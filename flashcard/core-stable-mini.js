const $=s=>document.querySelector(s);const S={p:'fc_vocab_progress_v2',r:'fc_vocab_show_reading_v2'};const e={list:$('#lessonList'),meta:$('#lessonMeta'),from:$('#fromInput'),to:$('#toInput'),limit:$('#limitSelect'),shuffle:$('#shuffleInput'),reading:$('#readingInput'),start:$('#startBtn'),title:$('#sessionTitle'),pos:$('#posText'),known:$('#knownText'),again:$('#againText'),bar:$('#progressBar'),card:$('#card'),front:$('#cardFront'),sub:$('#cardSub'),hint:$('#cardHint'),actions:$('#actions'),prev:$('#prevBtn'),next:$('#nextBtn'),flip:$('#flipBtn'),bad:$('#againBtn'),ok:$('#knownBtn'),review:$('#reviewBtn'),reset:$('#resetBtn'),log:$('#logBox')};const st={lessons:[],lesson:null,cards:[],session:[],i:0,face:0,known:new Set(),again:new Set(),showReading:false};window.e=e;window.st=st;function log(x){const t=new Date().toLocaleTimeString()+' · '+x;e.log.textContent=t+'\n'+e.log.textContent}function err(x){log('ERROR: '+(x&&x.message?x.message:x))}async function getText(url){log('fetch '+url);const r=await fetch(url,{cache:'no-store'});log('status '+r.status+' '+url);if(!r.ok)throw new Error(r.status+' '+url);return r.text()}const fallback={courses:[{title:'400 từ vựng N2 - Trường văn',lessons:[1,51,101,151,201,251,301,351].map((n,i)=>({id:`n2-reading-${String(n).padStart(3,'0')}-${String(n+49).padStart(3,'0')}`,title:`Bài ${String(i+1).padStart(2,'0')} · STT ${n}-${n+49}`,path:`./data/n2-reading-${String(n).padStart(3,'0')}-${String(n+49).padStart(3,'0')}.tsv`,count:50}))}]};async function loadManifest(){let m;try{m=JSON.parse(await getText('./data/manifest.json?v='+Date.now()))}catch(x){err(x);m=fallback}st.lessons=(m.courses||[]).flatMap(c=>(c.lessons||[]).map(l=>({...l,courseTitle:c.title})));renderMenu();log('lessons '+st.lessons.length)}function renderMenu(){e.list.innerHTML='';st.lessons.forEach((l,i)=>{const b=document.createElement('button');b.className='lesson-btn';b.type='button';b.innerHTML='<strong>'+l.title+'</strong><span>'+l.count+' thẻ</span>';b.onclick=()=>selectLesson(l.id);e.list.appendChild(b);if(i===0&&!st.lesson)setTimeout(()=>selectLesson(l.id),0)})}function parseTSV(t){const lines=t.trim().split(/\r?\n/).filter(Boolean);const h=lines.shift().split('\t');return lines.map(line=>{const v=line.split('\t'),o={};h.forEach((k,i)=>o[k]=v[i]||'');return{front:o.front,reading:o.reading,meaning_vi:o.meaning_vi,han_viet:o.han_viet,years:o.years}})}async function selectLesson(id){const l=st.lessons.find(x=>x.id===id);if(!l)return;try{st.lesson=l;document.body.classList.remove('library-open');document.querySelectorAll('.lesson-btn').forEach(b=>b.classList.toggle('active',b.textContent.includes(l.title)));const txt=await getText(l.path+'?v='+Date.now());st.cards=parseTSV(txt).map((c,i)=>({...c,no:i+1,id:l.id+'-'+(i+1)}));e.from.value=1;e.to.value=st.cards.length;e.meta.textContent=st.cards.length+' thẻ';st.session=[];st.i=0;st.face=0;loadProgress();render();log('selected '+l.title)}catch(x){err(x);e.title.textContent='Lỗi tải bài';e.front.textContent='Không tải được data';e.sub.textContent=x.message;e.hint.textContent='Mở Log để xem lỗi.'}}function loadProgress(){try{const all=JSON.parse(localStorage.getItem(S.p)||'{}'),p=all[st.lesson?.id]||{known:[],again:[]};st.known=new Set(p.known);st.again=new Set(p.again)}catch(x){}stats()}function saveProgress(){try{const all=JSON.parse(localStorage.getItem(S.p)||'{}');all[st.lesson.id]={known:[...st.known],again:[...st.again]};localStorage.setItem(S.p,JSON.stringify(all))}catch(x){}}function buildSession(){let a=Math.max(1,+e.from.value||1),b=Math.min(st.cards.length,+e.to.value||st.cards.length);if(a>b)[a,b]=[b,a];let arr=st.cards.filter(c=>c.no>=a&&c.no<=b);if(e.limit.value!=='all')arr=arr.slice(0,+e.limit.value);if(e.shuffle.checked)arr=[...arr].sort(()=>Math.random()-.5);return arr}function current(){return st.session[st.i]}function start(){if(!st.cards.length)return log('no cards');st.session=buildSession();st.i=0;st.face=0;render();log('start '+st.session.length)}function flip(){if(current()){st.face=(st.face+1)%2;render()}}function next(){if(!st.session.length)return;if(st.i<st.session.length-1){st.i++;st.face=0;render()}else log('end')}function prev(){if(st.session.length){st.i=Math.max(0,st.i-1);st.face=0;render()}}function mark(t){const c=current();if(!c)return;if(t==='known'){st.known.add(c.id);st.again.delete(c.id)}else{st.again.add(c.id);st.known.delete(c.id)}saveProgress();next()}function review(){st.session=st.cards.filter(c=>st.again.has(c.id));st.i=0;st.face=0;render();log('review '+st.session.length)}function reset(){if(!st.lesson||!confirm('Reset tiến độ bài này?'))return;st.known.clear();st.again.clear();saveProgress();render();log('reset')}function render(){const c=current();if(!c){e.title.textContent=st.lesson?'Sẵn sàng':'Chọn bài học';e.front.textContent=st.lesson?'Sẵn sàng':'Chưa có bài';e.sub.textContent=st.lesson?st.cards.length+' thẻ trong bài này.':'Đang tải data từ GitHub...';e.hint.textContent=st.lesson?'Bấm Bắt đầu học.':'Đang tải menu bài học.';e.actions.classList.add('hidden');stats();return}e.actions.classList.remove('hidden');e.title.textContent=st.lesson.title;if(st.face===0){e.front.textContent=c.front;e.sub.textContent=st.showReading?c.reading:'';e.hint.textContent='Mặt 1/2: từ.';$('#cardMeta').textContent='#n2'}else{e.front.textContent=c.meaning_vi;e.sub.textContent=[c.reading?'Cách đọc: '+c.reading:'',c.han_viet?'Âm Hán Việt: '+c.han_viet:''].filter(Boolean).join('\n');e.hint.textContent='Mặt 2/2: nghĩa.';$('#cardMeta').textContent=c.years?'Năm: '+c.years:'#n2'}stats()}function stats(){e.pos.textContent=st.session.length?(st.i+1)+'/'+st.session.length:'0/0';e.known.textContent='Biết: '+st.known.size;e.again.textContent='Chưa nhớ: '+st.again.size;e.bar.style.width=(st.session.length?(st.i+1)/st.session.length*100:0)+'%'}function bind(){$('#openMenu').onclick=()=>document.body.classList.add('library-open');$('#drawerBackdrop').onclick=()=>document.body.classList.remove('library-open');$('#debugToggle').onclick=()=>$('#logPanel').classList.toggle('hidden');e.reading.checked=localStorage.getItem(S.r)==='on';st.showReading=e.reading.checked;e.reading.onchange=()=>{st.showReading=e.reading.checked;localStorage.setItem(S.r,st.showReading?'on':'off');render()};e.start.onclick=start;e.card.onclick=flip;e.flip.onclick=flip;e.next.onclick=next;e.prev.onclick=prev;e.ok.onclick=()=>mark('known');e.bad.onclick=()=>mark('again');e.review.onclick=review;e.reset.onclick=reset;addEventListener('keydown',ev=>{if(ev.key==='Escape')document.body.classList.remove('library-open')})}Object.assign(window,{log,err,getText,loadManifest,renderMenu,parseTSV,selectLesson,loadProgress,saveProgress,buildSession,current,start,flip,next,prev,mark,review,reset,render,stats});bind();loadManifest().then(()=>render()).catch(x=>{err(x);e.front.textContent='App lỗi';e.sub.textContent=x.message});
