let audioCtx=null,musicTimer=null,isPlaying=false;function beep(freq,time,gainValue){const osc=audioCtx.createOscillator();const gain=audioCtx.createGain();osc.type='sine';osc.frequency.value=freq;gain.gain.setValueAtTime(.0001,time);gain.gain.exponentialRampToValueAtTime(gainValue,time+.03);gain.gain.exponentialRampToValueAtTime(.0001,time+.42);osc.connect(gain).connect(audioCtx.destination);osc.start(time);osc.stop(time+.46)}function startMusic(){audioCtx=audioCtx||new(window.AudioContext||window.webkitAudioContext)();const notes=[261.63,329.63,392,523.25,392,329.63];let step=0;musicTimer=setInterval(()=>{const now=audioCtx.currentTime;beep(notes[step%notes.length],now,.035);if(step%3===0)beep(notes[(step+2)%notes.length]/2,now,.02);step++},430);isPlaying=true;document.body.classList.add('music-on');document.querySelector('#musicBtn').textContent='Pause ♪';document.querySelector('#modeText').textContent='Music On';localStorage.setItem('duan_hub_music','on')}function stopMusic(){clearInterval(musicTimer);musicTimer=null;isPlaying=false;document.body.classList.remove('music-on');document.querySelector('#musicBtn').textContent='♪ Music';document.querySelector('#modeText').textContent='Silent Hub';localStorage.setItem('duan_hub_music','off')}document.querySelector('#musicBtn')?.addEventListener('click',async()=>{if(isPlaying)return stopMusic();startMusic();if(audioCtx.state==='suspended')await audioCtx.resume()});document.querySelectorAll('.app-card').forEach(card=>{card.addEventListener('pointermove',event=>{const rect=card.getBoundingClientRect();card.style.setProperty('--mx',`${event.clientX-rect.left-rect.width/2}px`);card.style.setProperty('--my',`${event.clientY-rect.top-rect.height/2}px`)})});document.addEventListener('click',event=>{const s=document.createElement('span');s.className='sparkle';s.textContent=['✦','✧','✨','★'][Math.floor(Math.random()*4)];s.style.left=event.clientX+'px';s.style.top=event.clientY+'px';document.body.appendChild(s);setTimeout(()=>s.remove(),700)});